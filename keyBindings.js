document.addEventListener("keydown", function(event){
    switch(event.code){
        case "Space":
            // remove selected
            break;
        default:
            console.log("unknown controls")   
    }
})